"use client";

import { LoadingSpinner } from "@/components/ui/loading-spinner";

const LoadingPage = () => {
  return <LoadingSpinner variant="overlay" size="lg" color="white" />;
};

export default LoadingPage;
